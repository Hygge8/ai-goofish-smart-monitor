# 设计取舍：集两家之长

本项目参考了两个优秀项目的方向，但不会简单堆功能，而是按“轻量可跑、通知好看、评分透明、后续可扩展”的原则重新整理。

## 参考来源

- `Usagi-org/ai-goofish-monitor`：Playwright + AI、多任务、Web 管理、SQLite、账号/代理轮换、Cron、结果查看。
- `banbanzhige/ai-goofish-monitor-QB`：三维评分、AI 视觉分析、卖家画像、评分看板、钉钉 ActionCard 图文通知、多用户思路。

两个项目均为 MIT License，二次开发需保留版权与许可说明。

## 取其精华

### 1. 保留轻量底座

先采用 Python + Playwright + SQLite + Docker，避免一开始就引入 PostgreSQL、多用户权限、复杂前端工程，降低部署门槛。

### 2. 保留多任务模型

每个任务独立配置：

- 关键词
- 价格区间
- 包含词
- 排除词
- 推荐分阈值
- 评分权重

### 3. 保留图文通知

钉钉通知使用 ActionCard：

- 顶部展示商品图
- 正文展示标题、价格、地区、评分、推荐理由
- 链接只放按钮，不在正文堆长链接

### 4. 引入透明评分体系

当前版本提供轻量评分器：

- 文本匹配分
- 价格分
- 卖家可信度分
- 图片可信度分
- 置信度
- 风险标签惩罚

最终输出综合评分和分项评分，方便后续接入真正的多模态 AI。

## 去其糟粕

### 1. 不过早引入重型数据库

多用户、PostgreSQL、角色权限在服务器团队使用时很有价值，但个人监控场景会增加部署成本。当前版本先保留 SQLite，后续再扩展。

### 2. 不强依赖 AI

AI Key 不稳定或成本高时，项目仍可通过规则评分正常运行。

### 3. 不把链接塞进正文

所有商品链接统一放到 ActionCard 按钮，避免群消息里出现很长的 PC / 手机端链接。

### 4. 不做复杂 UI 先行

当前先保证核心链路：抓取 -> 评分 -> 去重 -> 图文通知。Web 页面只做状态展示，后续再升级成完整管理后台。

## 下一步路线

1. 增加任务 Web 管理页面。
2. 增加 Cookie 登录态导入与账号池。
3. 增加代理池和风控失败重试。
4. 增加真正的 AI 多模态评分。
5. 增加企业微信、Telegram、自定义 Webhook。
6. 增加价格历史和降价提醒。
7. 增加一键标注“推荐 / 不推荐”，迭代评分规则。
